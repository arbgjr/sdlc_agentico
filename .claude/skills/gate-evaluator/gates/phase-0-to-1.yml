---
gate:
  name: "phase-0-to-1"
  from_phase: 0
  to_phase: 1
  description: "Preparation → Discovery transition"

  required_artifacts:
    - path: "{project_dir}/projects/{id}/manifest.yml"
      description: "Project manifest with ID, complexity level, and initial metadata"

  approval_required: false
  can_skip: false
