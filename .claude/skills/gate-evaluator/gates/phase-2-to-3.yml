---
gate:
  name: "phase-2-to-3"
  from_phase: 2
  to_phase: 3
  description: "Requirements → Architecture transition"

  required_artifacts:
    - path: "{project_dir}/projects/{id}/requirements/*.yml"
      description: "User stories with acceptance criteria"
    - path: "{project_dir}/projects/{id}/specs/*.spec.md"
      description: "Feature specification document"

  approval_required: false
  can_skip: false
