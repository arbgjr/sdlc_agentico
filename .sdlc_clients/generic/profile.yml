# SDLC Agêntico - Generic Client Profile
# Version: 3.0.0
# Purpose: Default profile (alias to base framework)

client:
  id: "generic"
  name: "Generic (Base Framework)"
  domain: "generic"
  version: "3.0.0"

  framework:
    min_version: "3.0.0"
    max_version: "99.99.99"

  # No detection markers (this is the fallback)
  detection:
    markers: []

# No overrides - use base framework entirely
agents:
  overrides: []
  additions: []

skills:
  overrides: []
  additions: []

gates:
  additions: []

templates:
  overrides: []
  additions: []

corpus:
  inherit_base: true
  client_only: false

hooks:
  overrides: []
